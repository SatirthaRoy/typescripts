const head = document.querySelector('h1');
console.log(head);

const area = (d: number)=> d*Math.PI;

let d:number = 12;

let obj: {
  name: string,
  age: number,
  belt: string
}

obj = {
  name: 'satirtha',
  age: 21,
  belt: 'black'
}

console.log(obj);
